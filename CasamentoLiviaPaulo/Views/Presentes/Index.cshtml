@model List<CasamentoLiviaPaulo.Models.Presente>

<link rel="stylesheet" href="~/css/Presentes.css" />

<h1 class="text-blossom text-rosa text-center font-78 mt-5 " style="line-height:1;">Presentes</h1>
<p class="text-azul text-center font-weight-bold">
    Querido convidado seja bem-vindo a nossa lista de presentes. Pensamos em tudo com muito carinho para que você se sinta à vontade para nos presentear com o que sentir vontade.
</p>

@*<div class="container-categorias">
    <a href="#" class="text-decoration-none text-azul font-weight-bold">
        <img src="~/img/icone-eletrodomesticos.png" />
        <span>Eletrodomesticos</span>
    </a>
    <a href="#" class="text-decoration-none text-azul font-weight-bold">
        <img src="~/img/icone-panelas.png" />
        <span>Panelas</span>
    </a>
    <a href="#" class="text-decoration-none text-azul font-weight-bold">
        <img src="~/img/icone-utensilios.png" />
        <span>Utensilios</span>
    </a>
    <a href="#" class="text-decoration-none text-azul font-weight-bold">
        <img src="~/img/icone-lazer.png" />
        <span>Lazer</span>
    </a>
</div>*@

<hr class="hr-azul" />

<div class="container-presentes row container mx-auto">
    @foreach (Presente presente in Model)
    {
        <div class="col-3">
            <div class="presente">
                <div class="imagens">
                    <span class="quantidade">@presente.Quantidade</span>
                    <span class="preco">R$ @presente.Preco</span>
                    <div class="owl-carousel">
                        @if (presente.base64 != null) {
                            foreach (String img in presente.base64)
                            {
                            <div style="width: 180px;display:flex;justify-content: center;">
                                <img src="data:image/png;base64, @img" style="height: 100px;width: 100px;" />
                            </div>
                            }
                        }
                    </div>
                </div>
                <span class="descricao">@presente.Nome</span>
                <a href="/Presentes/detalhe/@presente.Id" class="btn-rounded btn-azul text-white text-decoration-none">Presentear</a>
            </div>
        </div>
    }

</div>
<div class="paginacao">
    @if (Int32.Parse(ViewData["PaginaAtual"].ToString()) > 0)
    {
        <a href="/presentes?pagina=@(Int32.Parse(ViewData["PaginaAtual"].ToString())-1)">&lt;</a>
    }
    else
    {
        <a href="#">&lt;</a>
    }

    @for (int i = 0; i <= Model[0].Registros; i++)
    {
        @if (Int32.Parse(ViewData["PaginaAtual"].ToString())+1 == i+1)
        {
            <a href="/presentes?pagina=@(i)" class="paginacao-active">@(i + 1)</a>
        }
        else
        {
            <a href="/presentes?pagina=@(i)">@(i + 1)</a>
        }
    }

    @if (Int32.Parse(ViewData["PaginaAtual"].ToString()) < Model.Count / 20)
    {
        <a href="/presentes?pagina=@(Int32.Parse(ViewData["PaginaAtual"].ToString())+1)">></a>
    }
    else
    {
        <a href="#">></a>
    }
    @*<a href="/presentes?pagina=1" class="paginacao-active">1</a>
        <a href="/presentes?pagina=2">2</a>
        <a href="/presentes?pagina=3">3</a>
        <a href="/presentes?pagina=4">></a>*@
</div>

<script type="text/javascript">
    $(document).ready(function () {
        var owl = $('.owl-carousel');
        owl.owlCarousel({
            margin: 10,
            loop: true,
            center: true,
            autoWidth: true,
            responsive: {
                0: {
                    items: 1
                },
                600: {
                    items: 3
                },
                1000: {
                    items: 5
                }
            }
        })
    })
</script>